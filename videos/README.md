# videos

Drop video files here. The supervisor picks them up automatically and starts a stream per file. This directory is bind-mounted into the container by both the Compose and k8s deployments; the files themselves are gitignored.

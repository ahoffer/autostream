# videos

Drop video files here. The supervisor picks them up automatically and starts a
stream per file. This directory is bind-mounted into the container by Docker
Compose; the video files themselves are gitignored, and this README is kept so
a fresh clone has the directory.
